[report]
show_missing = True
exclude_lines =
    # Have to re-enable the standard pragma.
    pragma: no cover

    # Code that is run only when a script is executed directly.
    if __name__ == .__main__.:

    # Import errors.
    except ImportError:

[run]
relative_files = True
